Ali Mohamed:

1-) organized CSS into:
base → global styles (variables, reset)
components → reusable elements (navbar, buttons)
pages → page-specific styles
main.css → imports everything

2-) created a design system by:
defining colors (wine red, silver, etc.)
adding a reset for consistency
setting basic global styles

Mohamed Haitham:

Mohamed Muhanad:

Leen Badr: